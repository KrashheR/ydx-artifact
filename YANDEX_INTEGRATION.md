# Yandex Integration

`src/services/platform/mockPlatform.ts` is the current adapter seam. Real integration should add SDK initialization, player data save/load, lifecycle pause/resume, rewarded/interstitial and purchases behind the same service boundary.
